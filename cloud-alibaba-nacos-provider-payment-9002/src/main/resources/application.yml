server:
  port: 9002

spring:
  application:
    name: cloud-alibaba-nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'


